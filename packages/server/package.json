{
  "name": "@dak/server",
  "version": "0.5.0",
  "type": "module",
  "exports": {
    ".": "./src/worker.ts"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply dak --local",
    "migrate": "wrangler d1 migrations apply dak --remote",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@dak/contract": "workspace:*",
    "@hono/swagger-ui": "^0.6.1",
    "@hono/zod-openapi": "^1.3.0",
    "better-auth": "^1.6.2",
    "hono": "^4.7.0",
    "jieba-wasm": "^2.2.0",
    "kysely": "^0.28.2",
    "kysely-d1": "^0.4.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "typescript": "^5",
    "wrangler": "^4.0.0"
  }
}
